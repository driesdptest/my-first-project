# Rekenapp voor Kinderen Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Dutch-language math practice webapp for a 6-year-old with parent setup, exercise session, and results screens — no build step, deploys to GitHub Pages.

**Architecture:** Single-page app with three screens (shown/hidden via CSS class), vanilla JS split into a pure logic module (`exercises.js`) and a UI wiring module (`app.js`), plus a browser-based test page (`test.html`) for logic verification.

**Tech Stack:** HTML5, CSS3, JavaScript ES modules, Google Fonts (Nunito), no dependencies, no build step.

---

## File Map

| File | Responsibility |
|---|---|
| `index.html` | Markup for all three screens; loads `style.css` and `app.js` |
| `style.css` | All visual styles, animations (shake, confetti canvas), responsive layout |
| `exercises.js` | Pure functions: `generateExercise`, `generateSession` — no DOM, fully testable |
| `app.js` | UI wiring: screen transitions, settings validation, exercise flow, feedback, confetti |
| `test.html` | Browser test runner — imports `exercises.js` and runs assertions |

---

## Task 1: Scaffold project files

**Files:**
- Create: `index.html`
- Create: `style.css`
- Create: `exercises.js`
- Create: `app.js`
- Create: `test.html`

- [ ] **Step 1: Create index.html**

```html
<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Rekenapp</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <!-- Screen 1: Instellingen -->
  <div id="screen-instellingen" class="screen active">
    <h1>Rekenapp 🧮</h1>
  </div>

  <!-- Screen 2: Oefening -->
  <div id="screen-oefening" class="screen">
    <h1>Oefening</h1>
  </div>

  <!-- Screen 3: Resultaten -->
  <div id="screen-resultaten" class="screen">
    <h1>Resultaten</h1>
  </div>

  <script type="module" src="app.js"></script>
</body>
</html>
```

- [ ] **Step 2: Create style.css**

```css
/* Rekenapp stijlen */
```

- [ ] **Step 3: Create exercises.js**

```js
// Oefening logica
```

- [ ] **Step 4: Create app.js**

```js
// UI logica
import {} from './exercises.js';
```

- [ ] **Step 5: Create test.html**

```html
<!DOCTYPE html>
<html lang="nl">
<head><meta charset="UTF-8"><title>Tests</title></head>
<body><pre id="output"></pre>
<script type="module">
const out = document.getElementById('output');
function test(name, fn) {
  try { fn(); out.textContent += `✅ ${name}\n`; }
  catch(e) { out.textContent += `❌ ${name}: ${e.message}\n`; }
}
function assert(cond, msg) { if (!cond) throw new Error(msg || 'assertion failed'); }
</script>
</body>
</html>
```

- [ ] **Step 6: Open index.html in browser**

Drag `index.html` into your browser. You should see "Rekenapp 🧮" on a white page with no console errors.

- [ ] **Step 7: Commit**

```bash
git add index.html style.css exercises.js app.js test.html
git commit -m "feat: scaffold project files"
```

---

## Task 2: CSS foundation

**Files:**
- Modify: `style.css`

- [ ] **Step 1: Write base styles**

Replace `style.css` entirely with:

```css
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: 'Nunito', sans-serif;
  background: #f0f7ff;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 1rem;
}

.screen { display: none; width: 100%; max-width: 600px; }
.screen.active { display: block; }

h1 {
  font-size: 2.2rem;
  font-weight: 900;
  color: #1a73e8;
  text-align: center;
  margin-bottom: 1.5rem;
}

h2 {
  font-size: 1.4rem;
  font-weight: 700;
  color: #333;
  margin-bottom: 0.75rem;
}

.card {
  background: white;
  border-radius: 1.5rem;
  padding: 2rem;
  box-shadow: 0 4px 20px rgba(0,0,0,0.1);
}

button {
  font-family: 'Nunito', sans-serif;
  font-size: 1.4rem;
  font-weight: 700;
  padding: 0.9rem 2rem;
  border: none;
  border-radius: 999px;
  cursor: pointer;
  transition: transform 0.1s, opacity 0.2s;
}
button:active { transform: scale(0.96); }
button:disabled { opacity: 0.4; cursor: not-allowed; transform: none; }
.btn-primary { background: #1a73e8; color: white; width: 100%; margin-top: 1.5rem; }
.btn-secondary { background: #e8f0fe; color: #1a73e8; }

input[type="number"] {
  font-family: 'Nunito', sans-serif;
  font-size: 1.3rem;
  font-weight: 700;
  padding: 0.7rem 1rem;
  border: 3px solid #d0e4ff;
  border-radius: 0.75rem;
  width: 100%;
  outline: none;
  transition: border-color 0.2s;
}
input[type="number"]:focus { border-color: #1a73e8; }

.checkbox-group { display: flex; flex-wrap: wrap; gap: 0.75rem; margin-bottom: 1.25rem; }
.checkbox-label {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 1.2rem;
  font-weight: 700;
  cursor: pointer;
  background: #e8f0fe;
  padding: 0.6rem 1.1rem;
  border-radius: 999px;
  user-select: none;
}
.checkbox-label input { width: 1.2rem; height: 1.2rem; cursor: pointer; }

.number-row { display: flex; gap: 1rem; align-items: center; margin-bottom: 1.25rem; }
.number-row label { font-size: 1rem; font-weight: 700; color: #555; white-space: nowrap; }
.number-row input { width: 90px; flex-shrink: 0; }

.error-msg { color: #d93025; font-size: 0.95rem; font-weight: 700; margin-top: 0.4rem; min-height: 1.2rem; }

/* Progress bar */
.progress-wrap { margin-bottom: 1.5rem; }
.progress-label { font-size: 1rem; font-weight: 700; color: #555; margin-bottom: 0.4rem; text-align: center; }
.progress-bar { background: #d0e4ff; border-radius: 999px; height: 12px; }
.progress-fill { background: #1a73e8; border-radius: 999px; height: 12px; transition: width 0.4s ease; }

/* Problem display */
.problem { font-size: 3.5rem; font-weight: 900; color: #1a73e8; text-align: center; margin: 1.5rem 0; letter-spacing: 0.05em; }

/* Answer input */
.answer-wrap { display: flex; justify-content: center; margin-bottom: 1rem; }
.answer-input {
  font-size: 2.5rem !important;
  font-weight: 900 !important;
  text-align: center;
  width: 160px !important;
  padding: 0.5rem !important;
  border-width: 4px !important;
}

/* Feedback */
.feedback { font-size: 1.3rem; font-weight: 700; text-align: center; min-height: 2rem; margin-bottom: 0.75rem; }
.feedback.correct { color: #0d8f4e; }
.feedback.wrong { color: #d93025; }
.feedback.reveal { color: #555; }

/* Shake animation */
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  20% { transform: translateX(-10px); }
  40% { transform: translateX(10px); }
  60% { transform: translateX(-7px); }
  80% { transform: translateX(7px); }
}
.shake { animation: shake 0.4s ease; }

/* Results */
.score-display { font-size: 1.8rem; font-weight: 900; text-align: center; color: #333; margin-bottom: 1rem; }
.stars { font-size: 3rem; text-align: center; margin-bottom: 0.5rem; }
.result-msg { font-size: 1.2rem; font-weight: 700; text-align: center; color: #555; margin-bottom: 1.5rem; }
.btn-row { display: flex; gap: 1rem; justify-content: center; }
.btn-row button { width: auto; margin-top: 0; }

/* Confetti canvas */
#confetti-canvas {
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%;
  pointer-events: none;
  z-index: 100;
}
```

- [ ] **Step 2: Verify in browser**

Reload `index.html`. The page should have a light blue background, Nunito font, and "Rekenapp 🧮" in dark blue.

- [ ] **Step 3: Commit**

```bash
git add style.css
git commit -m "feat: add CSS foundation and design system"
```

---

## Task 3: Settings screen HTML

**Files:**
- Modify: `index.html`

- [ ] **Step 1: Replace settings screen content**

Replace the `<div id="screen-instellingen" ...>` block with:

```html
<div id="screen-instellingen" class="screen active">
  <h1>Rekenapp 🧮</h1>
  <div class="card">
    <h2>Bewerkingen</h2>
    <div class="checkbox-group">
      <label class="checkbox-label">
        <input type="checkbox" id="op-plus" value="+"> <span>Optellen (+)</span>
      </label>
      <label class="checkbox-label">
        <input type="checkbox" id="op-min" value="-"> <span>Aftrekken (−)</span>
      </label>
      <label class="checkbox-label">
        <input type="checkbox" id="op-keer" value="×"> <span>Vermenigvuldigen (×)</span>
      </label>
      <label class="checkbox-label">
        <input type="checkbox" id="op-delen" value="÷"> <span>Delen (÷)</span>
      </label>
    </div>

    <h2>Getallen</h2>
    <div class="number-row">
      <label for="min-getal">Van:</label>
      <input type="number" id="min-getal" value="1" min="1">
      <label for="max-getal">Tot:</label>
      <input type="number" id="max-getal" value="10" min="2">
    </div>
    <div class="error-msg" id="getal-error"></div>

    <h2>Aantal oefeningen</h2>
    <div class="number-row">
      <input type="number" id="aantal" value="10" min="1" max="50">
    </div>
    <div class="error-msg" id="aantal-error"></div>

    <button class="btn-primary" id="btn-starten" disabled>Starten! 🚀</button>
  </div>
</div>
```

- [ ] **Step 2: Verify in browser**

Reload. The settings screen should show four operation checkboxes, two number inputs (Van/Tot), a count input, and a disabled Starten button.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "feat: add settings screen HTML"
```

---

## Task 4: Settings validation and session start

**Files:**
- Modify: `app.js`

- [ ] **Step 1: Replace app.js**

```js
import { generateSession } from './exercises.js';

// --- State ---
let session = null;     // array of { a, op, b, answer }
let sessionIndex = 0;
let results = [];       // array of booleans (true = correct)
let settings = null;

// --- Settings screen elements ---
const btnStarten = document.getElementById('btn-starten');
const getalError = document.getElementById('getal-error');
const aantalError = document.getElementById('aantal-error');
const minInput = document.getElementById('min-getal');
const maxInput = document.getElementById('max-getal');
const aantalInput = document.getElementById('aantal');
const opCheckboxes = document.querySelectorAll('.checkbox-group input[type="checkbox"]');

// --- Validation ---
function validate() {
  const ops = [...opCheckboxes].filter(c => c.checked).map(c => c.value);
  const min = parseInt(minInput.value, 10);
  const max = parseInt(maxInput.value, 10);
  const aantal = parseInt(aantalInput.value, 10);
  let valid = true;

  if (isNaN(min) || isNaN(max) || min < 1 || max <= min) {
    getalError.textContent = 'Het maximale getal moet groter zijn dan het minimale getal (minimaal 1).';
    valid = false;
  } else {
    getalError.textContent = '';
  }

  if (isNaN(aantal) || aantal < 1 || aantal > 50) {
    aantalError.textContent = 'Kies een aantal tussen 1 en 50.';
    valid = false;
  } else {
    aantalError.textContent = '';
  }

  btnStarten.disabled = !(valid && ops.length > 0);
}

opCheckboxes.forEach(c => c.addEventListener('change', validate));
minInput.addEventListener('input', validate);
maxInput.addEventListener('input', validate);
aantalInput.addEventListener('input', validate);

// --- Start session ---
btnStarten.addEventListener('click', () => {
  const ops = [...opCheckboxes].filter(c => c.checked).map(c => c.value);
  settings = {
    ops,
    min: parseInt(minInput.value, 10),
    max: parseInt(maxInput.value, 10),
    aantal: parseInt(aantalInput.value, 10),
  };
  session = generateSession(settings);
  sessionIndex = 0;
  results = [];
  showScreen('screen-oefening');
  showExercise();
});

// --- Screen transitions ---
function showScreen(id) {
  document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
  document.getElementById(id).classList.add('active');
}

// --- Placeholders: filled in later tasks ---
function showExercise() {}
```

- [ ] **Step 2: Verify in browser**

Reload. Check one or more operation checkboxes and enter a valid min/max/count — the Starten button enables. Enter an invalid range (e.g., min=5, max=3) — error message appears and button stays disabled.

- [ ] **Step 3: Commit**

```bash
git add app.js
git commit -m "feat: add settings validation and session start logic"
```

---

## Task 5: Exercise generation logic

**Files:**
- Modify: `exercises.js`
- Modify: `test.html`

- [ ] **Step 1: Replace exercises.js**

```js
/**
 * Generate a single exercise object.
 * @param {{ ops: string[], min: number, max: number }} settings
 * @returns {{ a: number, op: string, b: number, answer: number }}
 */
export function generateExercise(settings) {
  const { ops, min, max } = settings;
  const op = ops[Math.floor(Math.random() * ops.length)];
  let a, b, answer;

  if (op === '+') {
    a = randInt(min, max);
    b = randInt(min, max);
    answer = a + b;
  } else if (op === '-') {
    a = randInt(min, max);
    b = randInt(min, max);
    if (b > a) [a, b] = [b, a];
    answer = a - b;
  } else if (op === '×') {
    a = randInt(min, max);
    b = randInt(min, max);
    answer = a * b;
  } else {
    // Division: generate answer and divisor first to guarantee whole numbers
    answer = randInt(min, max);
    b = randInt(min, max);
    if (b === 0) b = 1;
    a = answer * b;
    if (a > max) { b = 1; a = answer; }
  }

  return { a, op, b, answer };
}

/**
 * Generate a session of N unique exercises.
 * @param {{ ops: string[], min: number, max: number, aantal: number }} settings
 * @returns {Array<{ a: number, op: string, b: number, answer: number }>}
 */
export function generateSession(settings) {
  const seen = new Set();
  const exercises = [];
  let attempts = 0;
  const maxAttempts = settings.aantal * 20;

  while (exercises.length < settings.aantal && attempts < maxAttempts) {
    const ex = generateExercise(settings);
    const key = `${ex.a}${ex.op}${ex.b}`;
    if (!seen.has(key)) {
      seen.add(key);
      exercises.push(ex);
    }
    attempts++;
  }

  // If range too small for unique exercises, allow duplicates to fill remainder
  while (exercises.length < settings.aantal) {
    exercises.push(generateExercise(settings));
  }

  return exercises;
}

function randInt(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}
```

- [ ] **Step 2: Replace test.html**

```html
<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <title>Tests — Rekenapp</title>
  <style>body{font-family:monospace;padding:2rem;} .pass{color:green;} .fail{color:red;}</style>
</head>
<body>
<h2>Rekenapp Tests</h2>
<pre id="output"></pre>
<script type="module">
import { generateExercise, generateSession } from './exercises.js';

const out = document.getElementById('output');
let passed = 0, failed = 0;

function test(name, fn) {
  try {
    fn();
    out.innerHTML += `<span class="pass">✅ ${name}</span>\n`;
    passed++;
  } catch(e) {
    out.innerHTML += `<span class="fail">❌ ${name}: ${e.message}</span>\n`;
    failed++;
  }
}
function assert(cond, msg) { if (!cond) throw new Error(msg || 'mislukt'); }

const s = { ops: ['+', '-', '×', '÷'], min: 1, max: 10, aantal: 10 };

test('optelling: antwoord klopt', () => {
  for (let i = 0; i < 50; i++) {
    const ex = generateExercise({ ...s, ops: ['+'] });
    assert(ex.answer === ex.a + ex.b, `${ex.a}+${ex.b}≠${ex.answer}`);
  }
});

test('aftrekking: resultaat >= 0 en antwoord klopt', () => {
  for (let i = 0; i < 50; i++) {
    const ex = generateExercise({ ...s, ops: ['-'] });
    assert(ex.answer >= 0, `${ex.a}-${ex.b}=${ex.answer} < 0`);
    assert(ex.answer === ex.a - ex.b, `${ex.a}-${ex.b}≠${ex.answer}`);
  }
});

test('vermenigvuldigen: antwoord klopt', () => {
  for (let i = 0; i < 50; i++) {
    const ex = generateExercise({ ...s, ops: ['×'] });
    assert(ex.answer === ex.a * ex.b, `${ex.a}×${ex.b}≠${ex.answer}`);
  }
});

test('delen: geheel getal, niet door nul, antwoord klopt', () => {
  for (let i = 0; i < 50; i++) {
    const ex = generateExercise({ ...s, ops: ['÷'] });
    assert(ex.b !== 0, 'deling door nul');
    assert(Number.isInteger(ex.answer), `${ex.a}÷${ex.b}=${ex.answer} is geen geheel getal`);
    assert(ex.answer === ex.a / ex.b, `${ex.a}÷${ex.b}≠${ex.answer}`);
  }
});

test('optelling: getallen binnen bereik', () => {
  for (let i = 0; i < 50; i++) {
    const ex = generateExercise({ ...s, ops: ['+'] });
    assert(ex.a >= 1 && ex.a <= 10, `a=${ex.a} buiten bereik`);
    assert(ex.b >= 1 && ex.b <= 10, `b=${ex.b} buiten bereik`);
  }
});

test('sessie heeft juist aantal oefeningen', () => {
  const session = generateSession(s);
  assert(session.length === 10, `verwacht 10, kreeg ${session.length}`);
});

test('sessie heeft unieke oefeningen', () => {
  const session = generateSession(s);
  const keys = session.map(e => `${e.a}${e.op}${e.b}`);
  const unique = new Set(keys);
  assert(unique.size === keys.length, `duplicaten gevonden`);
});

out.innerHTML += `\n<strong>${passed} geslaagd, ${failed} mislukt</strong>\n`;
</script>
</body>
</html>
```

- [ ] **Step 3: Open test.html in browser**

Open `test.html`. All 7 tests should show green checkmarks. If any fail, check `exercises.js`.

- [ ] **Step 4: Commit**

```bash
git add exercises.js test.html
git commit -m "feat: add exercise generation logic and tests"
```

---

## Task 6: Exercise screen HTML and interaction logic

**Files:**
- Modify: `index.html`
- Modify: `app.js`

- [ ] **Step 1: Replace exercise screen content in index.html**

Replace the `<div id="screen-oefening" ...>` block with:

```html
<div id="screen-oefening" class="screen">
  <h1>Rekenapp 🧮</h1>
  <div class="card">
    <div class="progress-wrap">
      <div class="progress-label" id="progress-label">Vraag 1 van 10</div>
      <div class="progress-bar"><div class="progress-fill" id="progress-fill" style="width:0%"></div></div>
    </div>
    <div class="problem" id="problem-display">7 + 4 = ?</div>
    <div class="answer-wrap">
      <input type="number" id="answer-input" class="answer-input" placeholder="?" autocomplete="off">
    </div>
    <div class="feedback" id="feedback-msg"></div>
    <button class="btn-primary" id="btn-controleren">Controleren ✅</button>
  </div>
</div>
```

- [ ] **Step 2: Replace the placeholder `function showExercise() {}` in app.js**

Add the following block in place of `// --- Placeholders: filled in later tasks ---` and `function showExercise() {}`:

```js
// --- Exercise screen elements ---
const problemDisplay = document.getElementById('problem-display');
const answerInput = document.getElementById('answer-input');
const feedbackMsg = document.getElementById('feedback-msg');
const progressLabel = document.getElementById('progress-label');
const progressFill = document.getElementById('progress-fill');
const btnControleren = document.getElementById('btn-controleren');

const CELEBRATION_MSGS = [
  'Goed zo! 🎉', 'Super! ⭐', 'Wauw, geweldig! 🌟',
  'Fantastisch! 🏆', 'Jij bent een rekenster! 🌈', 'Bravo! 👏'
];

let attempts = 0;

function showExercise() {
  const ex = session[sessionIndex];
  problemDisplay.textContent = `${ex.a} ${ex.op} ${ex.b} = ?`;
  answerInput.value = '';
  feedbackMsg.textContent = '';
  feedbackMsg.className = 'feedback';
  attempts = 0;
  updateProgress();
  setTimeout(() => answerInput.focus(), 100);
}

function updateProgress() {
  const current = sessionIndex + 1;
  const total = session.length;
  progressLabel.textContent = `Vraag ${current} van ${total}`;
  progressFill.style.width = `${((current - 1) / total) * 100}%`;
}

function nextExercise() {
  sessionIndex++;
  if (sessionIndex >= session.length) {
    showResults();
  } else {
    showExercise();
  }
}

btnControleren.addEventListener('click', checkAnswer);
answerInput.addEventListener('keydown', e => { if (e.key === 'Enter') checkAnswer(); });

function checkAnswer() {
  const userAnswer = parseInt(answerInput.value, 10);
  if (isNaN(userAnswer)) { answerInput.focus(); return; }

  const correct = session[sessionIndex].answer;

  if (userAnswer === correct) {
    results.push(true);
    feedbackMsg.textContent = CELEBRATION_MSGS[Math.floor(Math.random() * CELEBRATION_MSGS.length)];
    feedbackMsg.className = 'feedback correct';
    launchConfetti();
    btnControleren.disabled = true;
    answerInput.disabled = true;
    setTimeout(() => {
      btnControleren.disabled = false;
      answerInput.disabled = false;
      nextExercise();
    }, 1500);
  } else {
    attempts++;
    if (attempts < 3) {
      const msgs = ['Probeer nog eens! 💪', 'Bijna! Probeer nog één keer! 🤔'];
      feedbackMsg.textContent = msgs[Math.min(attempts - 1, msgs.length - 1)];
      feedbackMsg.className = 'feedback wrong';
      triggerShake(answerInput);
      answerInput.value = '';
      answerInput.focus();
    } else {
      results.push(false);
      feedbackMsg.textContent = `Het antwoord is ${correct}`;
      feedbackMsg.className = 'feedback reveal';
      btnControleren.disabled = true;
      answerInput.disabled = true;
      setTimeout(() => {
        btnControleren.disabled = false;
        answerInput.disabled = false;
        nextExercise();
      }, 2000);
    }
  }
}

function triggerShake(el) {
  el.classList.remove('shake');
  void el.offsetWidth; // force reflow to restart animation
  el.classList.add('shake');
  el.addEventListener('animationend', () => el.classList.remove('shake'), { once: true });
}

// Placeholders replaced in Tasks 7 and 8
function launchConfetti() {}
function showResults() {}
```

- [ ] **Step 3: Verify in browser**

Reload. Click Starten (after selecting an operation and valid numbers). The exercise screen should appear with a problem and progress bar. Enter a correct answer — celebration message. Enter a wrong answer — shake and retry message. After 3 wrong attempts — correct answer shown, then next exercise loads.

- [ ] **Step 4: Commit**

```bash
git add index.html app.js
git commit -m "feat: add exercise screen HTML and interaction logic"
```

---

## Task 7: Confetti animation

**Files:**
- Modify: `index.html`
- Modify: `app.js`

- [ ] **Step 1: Add canvas element to index.html**

Add this line just before `</body>` (before the `<script>` tag):

```html
<canvas id="confetti-canvas"></canvas>
```

- [ ] **Step 2: Replace `function launchConfetti() {}` in app.js**

Find `function launchConfetti() {}` and replace it with:

```js
const confettiCanvas = document.getElementById('confetti-canvas');
const ctx = confettiCanvas.getContext('2d');
let confettiParticles = [];
let confettiAnimating = false;
const CONFETTI_COLORS = ['#1a73e8','#fbbc04','#34a853','#ea4335','#ff6d00','#ab47bc'];

function launchConfetti(fullBurst = false) {
  confettiCanvas.width = window.innerWidth;
  confettiCanvas.height = window.innerHeight;
  const count = fullBurst ? 180 : 80;
  for (let i = 0; i < count; i++) {
    confettiParticles.push({
      x: Math.random() * confettiCanvas.width,
      y: Math.random() * confettiCanvas.height * 0.4,
      w: 8 + Math.random() * 8,
      h: 5 + Math.random() * 5,
      color: CONFETTI_COLORS[Math.floor(Math.random() * CONFETTI_COLORS.length)],
      vx: (Math.random() - 0.5) * 4,
      vy: 2 + Math.random() * 4,
      angle: Math.random() * 360,
      spin: (Math.random() - 0.5) * 6,
      alpha: 1,
    });
  }
  if (!confettiAnimating) animateConfetti();
}

function animateConfetti() {
  confettiAnimating = true;
  ctx.clearRect(0, 0, confettiCanvas.width, confettiCanvas.height);
  confettiParticles = confettiParticles.filter(p => p.alpha > 0.05);
  confettiParticles.forEach(p => {
    p.x += p.vx;
    p.y += p.vy;
    p.angle += p.spin;
    p.alpha -= 0.012;
    ctx.save();
    ctx.globalAlpha = Math.max(0, p.alpha);
    ctx.translate(p.x, p.y);
    ctx.rotate((p.angle * Math.PI) / 180);
    ctx.fillStyle = p.color;
    ctx.fillRect(-p.w / 2, -p.h / 2, p.w, p.h);
    ctx.restore();
  });
  if (confettiParticles.length > 0) {
    requestAnimationFrame(animateConfetti);
  } else {
    ctx.clearRect(0, 0, confettiCanvas.width, confettiCanvas.height);
    confettiAnimating = false;
  }
}
```

- [ ] **Step 3: Verify in browser**

Reload. Enter a correct answer — colorful confetti should burst across the screen and fade out smoothly.

- [ ] **Step 4: Commit**

```bash
git add index.html app.js
git commit -m "feat: add confetti animation"
```

---

## Task 8: Results screen HTML and logic

**Files:**
- Modify: `index.html`
- Modify: `app.js`

- [ ] **Step 1: Replace results screen content in index.html**

Replace the `<div id="screen-resultaten" ...>` block with:

```html
<div id="screen-resultaten" class="screen">
  <h1>Klaar! 🎊</h1>
  <div class="card">
    <div class="stars" id="stars-display">⭐⭐⭐</div>
    <div class="score-display" id="score-display">Je hebt 8 van de 10 sommen goed!</div>
    <div class="result-msg" id="result-msg">Geweldig!</div>
    <div class="btn-row">
      <button class="btn-secondary" id="btn-instellingen">⚙️ Instellingen</button>
      <button class="btn-primary" id="btn-opnieuw" style="width:auto;margin-top:0">🔄 Opnieuw</button>
    </div>
  </div>
</div>
```

- [ ] **Step 2: Replace `function showResults() {}` in app.js**

Find `function showResults() {}` and replace it with:

```js
const starsDisplay = document.getElementById('stars-display');
const scoreDisplay = document.getElementById('score-display');
const resultMsg = document.getElementById('result-msg');
const btnOpnieuw = document.getElementById('btn-opnieuw');
const btnInstellingen = document.getElementById('btn-instellingen');

function showResults() {
  const correct = results.filter(Boolean).length;
  const total = results.length;
  const pct = correct / total;

  scoreDisplay.textContent = `Je hebt ${correct} van de ${total} sommen goed!`;

  if (pct >= 0.8) {
    starsDisplay.textContent = '⭐⭐⭐';
    resultMsg.textContent = 'Geweldig! Je bent een rekenkampioen! 🏆';
    launchConfetti(true);
  } else if (pct >= 0.5) {
    starsDisplay.textContent = '⭐⭐';
    resultMsg.textContent = 'Goed gedaan! Blijf zo doorgaan!';
  } else {
    starsDisplay.textContent = '☆☆☆';
    resultMsg.textContent = 'Blijf oefenen, je komt er!';
  }

  showScreen('screen-resultaten');
}

btnOpnieuw.addEventListener('click', () => {
  session = generateSession(settings);
  sessionIndex = 0;
  results = [];
  showScreen('screen-oefening');
  showExercise();
});

btnInstellingen.addEventListener('click', () => {
  showScreen('screen-instellingen');
});
```

- [ ] **Step 3: Verify full flow in browser**

Reload. Complete a full session. The results screen should show:
- Score text ("Je hebt X van de Y sommen goed!")
- Star rating matching your score (☆☆☆ / ⭐⭐ / ⭐⭐⭐)
- Correct Dutch message per tier
- Full confetti burst if score ≥ 80%
- Opnieuw replays with same settings; Instellingen goes back to setup

- [ ] **Step 4: Commit**

```bash
git add index.html app.js
git commit -m "feat: add results screen HTML and logic"
```

---

## Task 9: Final checks and GitHub Pages deploy

**Files:**
- Create: `.gitignore`

- [ ] **Step 1: Full manual test checklist**

Open `index.html` and verify each scenario:

1. No operation checked → Starten stays disabled ✓
2. min=5, max=3 → error message appears ✓
3. count=0 → error message appears ✓
4. Complete a session with all four operations ✓
5. Answer wrong 3 times → correct answer shown, next exercise loads after 2s ✓
6. Score ≥ 80% → three stars + full confetti ✓
7. Score 50–79% → two stars, no confetti ✓
8. Score < 50% → empty stars, encouraging message ✓
9. Opnieuw → replays same settings ✓
10. Instellingen → returns to setup screen ✓

- [ ] **Step 2: Run tests**

Open `test.html`. Confirm all 7 tests pass green.

- [ ] **Step 3: Add .gitignore and commit**

```bash
echo ".DS_Store" >> .gitignore
echo ".superpowers/" >> .gitignore
git add .gitignore
git commit -m "chore: add .gitignore"
```

- [ ] **Step 4: Push to GitHub**

```bash
git push
```

- [ ] **Step 5: Enable GitHub Pages**

Go to `https://github.com/driesdptest/my-first-project` → **Settings** → **Pages** → Source: **Deploy from branch** → Branch: `main` → folder: `/ (root)` → **Save**.

After ~1 minute the app is live at `https://driesdptest.github.io/my-first-project/`.
